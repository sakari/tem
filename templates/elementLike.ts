export class {{{className}}} extends {{{extending}}} {
    _is{{kind}}: boolean;

    constructor() {
        super('{{{tagName}}}')
    }

{{{element className siblings}}}
{{#if valueType}}
{{{value valueType}}}
{{/if}}
}